/* 

GLOBALS - NO WINDOW
__dirname = path to current diretory
__filename = file name
require = function to use module (common JS)
module = function to show info about the module (file)
process = info about environment where the program is executed

*/


console.log(__dirname);

setInterval(() => {
    console.log(`hello there`);
},1000);